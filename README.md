# AI-Skin-Specialist

## Keep API keys safe before pushing to GitHub

1. Copy sample.env to .env
2. Put your real keys only in .env
3. Never hardcode keys in Python files

This repository ignores .env and other local env secret files via .gitignore, so your keys stay local.